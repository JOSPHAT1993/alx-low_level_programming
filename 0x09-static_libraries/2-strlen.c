#include "main.h"
/**
 * _strlen-main netry function for printing the length of a string
 *
 * @s:'String Variable'
 *
 * Return:The string of characters
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}

