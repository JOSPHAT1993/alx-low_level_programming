#include "main.h"
/**
 * strcat-main function for concatenating strings
 *
 * @dest:'String Variable used'
 * @src:'String Variable'
 *
 * Return:Concatenated strings to one string
 */
char *_strcat(char *dest, char *src)
{
	int i, j;

	for (i = 0; i <= dest[i]; i++)
	{
	}
	for (j = 0; j <= src[j]; j++)
	{
		dest[i + j] = src[j];
	}
	dest[i + j] ='\0';
	return (dest);
}
