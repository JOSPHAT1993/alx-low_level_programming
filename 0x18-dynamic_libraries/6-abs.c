#include "main.h"
/**
 * _abs-main function
 *
 * @n:'Integer variable'
 *
 * Return:int for positive numbers printed while negative multiply with -1
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
